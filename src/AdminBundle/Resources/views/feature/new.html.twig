{% extends 'AdminBundle::layout.html.twig' %}

{% block body %}
    <div class="content-wrapper">
        <div class="content-header">
            <h1>
                Ajouter Feature
                <a class="btn btn-primary pull-right" href="{{ path('admin_feature_index') }}">Retourner à la liste</a>
            </h1>
        </div>
        <div class="content">
            <div class="box">
                <div class="box-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input class="btn btn-success pull-right" type="submit" value="Ajouter" />
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}
